pipeline {
    agent none
    stages {
            stage('test') {
            agent any
            steps {
                withCredential ([usernamePassword(credentialsId: 'webserver_login', usernameVariable: 'USERNAME', passwordVariable: 'USERPASS')]) { 
                sshagent ( ['webserver_login']) {
    sh '''
ssh -vv coni@34.245.222.45 echo testing connection || true
ssh-add -L
echo done running remote server test
'''

  }
  }
            }
        }


}
}

